
// AI Service removed to prevent errors and ensure offline functionality.

export const generateIdeas = async (prompt: string, context: string): Promise<string> => {
  return "La asistencia por IA está desactivada en esta versión.";
};

export const analyzeMenu = async (menuItems: string[]): Promise<string> => {
  return "Análisis por IA desactivado.";
};
